# Meta Ads Dashboard

This repo now includes a single-file dashboard (`dashboard.html`) that can be opened directly from the filesystem and renders Meta Ads KPIs, trend chart, campaign tables, top/bottom ads, and a funnel view using embedded CSV data.

## Open locally

```bash
xdg-open dashboard.html
```

No web server is required.
